import ProfilePageTmpl from "@/components/templates/ProfilePageTmpl";

/**
 * Page-level wrapper (atomic design) around {@link ProfilePageTmpl}.
 *
 * @returns The profile screen.
 */
export default function ProfilePage() {
    return (
        <ProfilePageTmpl />
    );
}