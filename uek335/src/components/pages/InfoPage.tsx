import InfoPageTmpl from "@/components/templates/InfoPageTmpl";

/**
 * Page-level wrapper (atomic design) around {@link InfoPageTmpl}.
 *
 * @returns The info/landing screen.
 */
export default function InfoPage() {
    return (
        <InfoPageTmpl />
    );
}