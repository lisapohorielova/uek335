import RegisterPageTmpl from "@/components/templates/RegisterPageTmpl";

/**
 * Page-level wrapper (atomic design) around {@link RegisterPageTmpl}.
 *
 * @returns The register screen.
 */
export default function RegisterPage() {
    return (
        <RegisterPageTmpl />
    );
}