import LoginPageTmpl from "@/components/templates/LoginPageTmpl";

/**
 * Page-level wrapper (atomic design) around {@link LoginPageTmpl}.
 *
 * @returns The login screen.
 */
export default function LoginPage() {
    return (
        <LoginPageTmpl />
    );
}