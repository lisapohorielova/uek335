import BooksPage from "@/components/pages/BooksPage";

/**
 * `/books` route. Renders the {@link BooksPage}.
 *
 * @returns The books page.
 */
export default function Books() {
    return (
        <BooksPage />
    );
}
