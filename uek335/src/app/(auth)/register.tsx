import RegisterPage from "@/components/pages/RegisterPage";

/**
 * `register` route. Renders the {@link RegisterPage}.
 *
 * @returns The register page.
 */
export default function Register() {
    return <RegisterPage />;
}
