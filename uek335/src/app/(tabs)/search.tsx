import BooksPage from "@/components/pages/BooksPage";

/**
 * `search` tab route. Renders the {@link BooksPage} (library with search/filter).
 *
 * @returns The books page.
 */
export default function Search() {
    return <BooksPage />;
}
